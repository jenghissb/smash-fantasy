import fantasy from "@/config/fantasy.json";

export const fantasyConfig = {
  budget: fantasy.budget,
  maxRosterSize: fantasy.maxRosterSize,
  allowPartialRoster: fantasy.allowPartialRoster,
  titleSuffix: fantasy.titleSuffix,
};
